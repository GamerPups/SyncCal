# Frontend — leave empty to use same-origin /api (Vite proxy in dev, Vercel in prod)
VITE_API_BASE_URL=

# Google OAuth (server)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# App URLs
APP_URL=http://localhost:5173
API_URL=http://localhost:3001

# Server port (local dev)
PORT=3001
